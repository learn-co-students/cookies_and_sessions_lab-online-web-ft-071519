<h1> Your cart: </h1>

<ul>
  <% cart.each do |item| %>
    <%= item %>
  <% end %>
</ul>

<h2> Add an item to your cart: </h2>

<%= form_tag("/", method: "post") do %>
  <%= label_tag(:product) %>
  <%= text_field_tag(:product) %>
  <%= submit_tag("add to cart") %>
<% end %>